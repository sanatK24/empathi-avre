# Frontend Configuration - Copy to .env.local and fill in actual values
VITE_API_BASE_URL=http://localhost:8000
VITE_APP_NAME=AVRE

# Optional: Google OAuth (if implementing OAuth)
# VITE_GOOGLE_CLIENT_ID=your-google-client-id
