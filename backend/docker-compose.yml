version: '3.8'

services:
  web:
    build: .
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./avre.db
      - SECRET_KEY=DEVELOPMENT_ONLY_INSECURE_KEY
      - NODE_ENV=development
    restart: always
